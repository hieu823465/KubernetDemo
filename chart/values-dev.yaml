# Development environment overrides
replicaCount: 1

environment: development

# Disable NodePort to avoid port conflicts with other releases
nodePort:
  enabled: false

resources:
  requests:
    memory: "32Mi"
    cpu: "50m"
  limits:
    memory: "64Mi"
    cpu: "100m"
